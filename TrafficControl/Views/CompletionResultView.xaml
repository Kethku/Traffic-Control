<UserControl x:Class="TrafficControl.Views.CompletionResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrafficControl.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="2" BorderBrush="{Binding BorderBrush}" Padding="12">
        <StackPanel Orientation="Horizontal">
            <Label Foreground="White" FontSize="20" Content="{Binding CompletionDisplay}"/>
            <Label Foreground="#606060" FontSize="20" Content="{Binding CompletionDescription}"/>
        </StackPanel>
    </Border>
</UserControl>

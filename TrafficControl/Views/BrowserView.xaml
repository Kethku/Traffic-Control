<Window x:Class="TrafficControl.Views.BrowserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="Simple Browser" Height="450" Width="800"
        Background="Gray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0">Address: </Label>
        <TextBox 
            x:Name="AddressInput"
            Grid.Column="1" 
            Grid.Row="0" 
            Margin="2"
            cal:Message.Attach="[Key Enter] = [Action NavigateTo]"/>
        <cef:ChromiumWebBrowser 
            x:Name="Cef" 
            Grid.Row="1" 
            Grid.Column="0"  
            Grid.ColumnSpan="2"
            Address="{Binding Address, Mode=TwoWay}"/>
    </Grid>
</Window>
